import { Buttons } from './CollectionButtons.styled';

export const CollectionButtons = () => {
  return (
    <Buttons>
      <div>
        <button type="button">Not in the collection</button>
      </div>
      <div>
        <button type="button">Want to read</button>
      </div>
    </Buttons>
  );
};
